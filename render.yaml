services:
  - type: web
    name: super-mars-studio-chat
    runtime: node
    plan: free
    region: singapore
    buildCommand: echo No build required
    startCommand: node server.js
    healthCheckPath: /health
